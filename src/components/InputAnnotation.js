import React from 'react';
import Typography from '@mui/material/Typography';

const InputAnnotation = ({ helper }) => {
    return (
        <Typography>
            {helper}
        </Typography>
    );
};

export default InputAnnotation;
